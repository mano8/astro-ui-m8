// Shared test harness for @mano8/astro-ui-m8 consumers (the "./testing" export).
//
// Provides the query-hook + component harness reused across the astro-*-m8
// plugins: mock `request`/auth-adapter, a `QueryClientProvider` wrapper, and
// render helpers covering loading / error / empty / success / unauthorized plus
// mutation success/failure. Populated in a later Phase 1 step.
export {};
